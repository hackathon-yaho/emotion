# 감정 케어 보이스 저널 — 문서

> **수정 기록 (2026-09-05 ㊸, AI)** — **LLM 벤더를 Google Gemini 무료 티어로 바꿨습니다.** 크레딧 소진에 따른 팀 결정입니다. 오늘만 두 번째 교체인데, **두 번 다 설계·계약·프롬프트·가드는 한 줄도 안 바뀌었습니다** — LLM 호출을 `app/llm/` 한 계층에 가둬 둔 경계가 값을 했습니다.
>
> **① 백엔드·앱 필독 — 환경변수가 또 바뀌었습니다.** `OPENAI_API_KEY`가 아니라 **`GOOGLE_API_KEY`** 입니다([AI Studio](https://aistudio.google.com/apikey)에서 발급, 무료). 모델은 분석 `gemini-3.5-flash-lite` · 응답 `gemini-3.8-flash` · 관찰 `gemini-2.5-pro` · 요약 `gemini-3.5-flash-lite`. **Gemini가 OpenAI 호환 엔드포인트를 제공해서 SDK는 그대로 두고 `base_url`만 바꿨습니다** — 네이티브 SDK로 갈아타면 네 호출부를 다시 쓰고 스트리밍을 다시 검증해야 하는데, 호환 계층이 스트리밍·`response_format`·`reasoning_effort`를 모두 지원해 **환경변수 세 줄**로 끝났습니다. `backend/docs/`에 옛 키 이름이 남아 있습니다.
>
> **② 무료 티어의 제약은 비용이 아니라 분당 요청 수입니다** (`ai-pipeline.md` §2.9 신설). 턴 하나가 LLM을 2번 부르므로(분석+응답) 대화 하나가 **분당 약 8건**을 씁니다. **3인이 동시에 말하면 약 24건이라 한 키로는 넘칠 가능성이 높습니다.** 그리고 **"각자 자기 무료 키를 쓴다"는 이 구조에서 성립하지 않습니다** — AI서버는 한 대이고 키를 하나 들고 있어서 누가 말하든 같은 키로 나갑니다. 사용자별 키 라우팅은 만들지 않습니다. **그래서 도그푸딩은 3인이 동시에 하지 않고 시간을 나눠서 합니다.** 한도를 넘으면 분석은 빈 결과, 응답은 정형 문장으로 떨어지고 **대화는 멈추지 않습니다.** 다만 원인을 알 수 있도록 `llm_rate_limited`를 다른 실패와 구분해 남깁니다. 정확한 한도는 공식 문서가 표로 주지 않고 콘솔을 가리켜서, PRD §2.5에 따라 3자 수치를 확정값으로 쓰지 않고 **AI Studio에서 확인**합니다.
>
> **③ 시크릿을 넣는 스크립트를 만들었습니다** — `ai-server/scripts/set-secret.ps1`. 값이 **화면에도 명령 기록에도 남지 않습니다.** 따옴표째 붙여넣기, `이름=값` 통째 붙여넣기, 줄바꿈 섞임을 먼저 걸러냅니다. 그런 실수는 나중에 401을 디버깅하게 만듭니다.
>
> 테스트 194건.
>
> **수정 기록 (2026-09-05 ㊷, AI)** — **LLM 벤더를 OpenAI로 교체했고, 통합 1차 결함 2건에 회신했습니다(✅ `request/ai/integration-round-1.md`).**
>
> **① 백엔드·앱 필독 — 환경변수 이름이 바뀌었습니다.** `ANTHROPIC_API_KEY`가 아니라 **`OPENAI_API_KEY`** 입니다(PRD §14-8, `ai-pipeline.md` AI-20). 모델은 분석 `gpt-5.6-luna` · 응답 `gpt-5.6-terra` · 관찰 `gpt-5.6-sol` · 요약 `gpt-5.6-luna`. **설계·계약·프롬프트·가드는 하나도 바뀌지 않았습니다** — 고친 범위는 `app/llm/` 4개 파일과 환경변수뿐이고, LLM 호출을 한 계층에 가둬 둔 경계가 실제로 값을 했습니다. `backend/docs/blocked.md`·`phase-4-pattern-batch.md`에 옛 이름이 남아 있어 정정을 요청드렸습니다. 모델별로 받는 파라미터가 다르고 문서에 다 적혀 있지도 않아, **400을 맞으면 그 파라미터를 빼고 한 번 다시 시도하고 기억하도록** 했습니다(AI-21) — 라인업이 바뀌어도 첫 호출로 서버가 죽지 않습니다.
>
> **② `sessionId`가 로그에 평문으로 남고 있었습니다. 고쳤습니다.** 백엔드가 통합 1차에서 30분에 7건을 발견했습니다. 계약 §1-1이 이 값을 "비밀과 동급"으로 규정한 이유는 **그게 곧 CLM 인증 수단**이기 때문입니다 — 로그를 본 사람이 그 세션인 척 대화를 이어갈 수 있습니다. 제가 만든 필드 화이트리스트에 `sid`가 들어 있어서 **화이트리스트가 오히려 유출을 승인한 꼴**이었습니다. **`sessionRef` = `SHA-256(sessionId)[:8]`** 로 바꿨고(백엔드와 같은 방식이라 양쪽 로그를 한 세션으로 맞춰 볼 수 있습니다), 다음에 다른 이름으로 넘겨도 새지 않도록 **`sid`·`sessionId`·`session_id`·`customSessionId`·`custom_session_id`를 이름 단위로 거부**합니다. 실행 로그에서 원본 0건을 확인했습니다.
>
> **③ fail-closed 경로 500은 요청서보다 먼저 고쳐져 있었습니다**(`43c97be`). 같은 실수가 `session.py`에도 있어 같이 고쳤고, 401 경로 3종(없는 세션 · `ended` 세션 · 세션 ID 없음)을 종단 테스트로 고정했습니다. **정상 경로에서는 절대 안 밟히는 코드라 통합 전까지 안 드러났다**는 백엔드의 지적이 이 문제의 핵심이었습니다.
>
> 테스트 192건. AI서버 포트는 `8100`이 맞습니다.
>
> **수정 기록 (2026-09-05 ㊹, 앱)** — **EVI 음성 서비스를 구현했습니다**(`app/lib/core/voice/`) **+ 앱에 온 요청 2건 회신(✅ `chat-group-id.md` · `kakao-rest-key-switch.md`).** 소켓·마이크·재생을 각각 인터페이스로 두고 **가짜 소켓으로 17건을 검증**했습니다 — 핸드셰이크(토큰·Config·`custom_session_id`·이어하기), 수신(`chat_metadata`·`user_message`·`audio_output`·`user_interruption`·`error`), 실패 경로, 종료. 코드에 못 박은 것들: **`language_model_api_key`를 절대 보내지 않고**(웹 번들 노출 — 테스트가 이 문자열이 나가지 않는지 봅니다), **프로소디를 파싱하지 않으며**(사건 클래스에 점수를 담을 자리 자체가 없습니다 — 들고 있으면 언젠가 화면에 나옵니다), **음성을 파일로 쓰지 않고**(`startStream`으로 받아 소켓으로만), **어떤 실패에서도 예외를 밖으로 던지지 않습니다**(F2-04). 소켓을 먼저 열고 마이크를 나중에 켭니다 — 순서를 뒤집으면 인증 실패인데도 권한 창이 먼저 떠 원인을 오해합니다. **마이크 음성 왕복은 여전히 미검증입니다**(PRD §14-4) — 토큰이 `session/start`에서만 나와 백엔드가 붙는 날 확인합니다. **`chat-group-id` 회신**: `chat_group_id`는 **소켓이 열린 직후 `chat_metadata`로** 옵니다 — 값이 그때 손에 있으니 3안(연결 직후 전송)으로 부탁했고, 강제 종료를 덮는 것이 그것뿐입니다. 덧붙여 **§2-5-1 `resumedChatGroupId`가 필수 필드인데 값이 없을 때가 정해져 있지 않다**는 점을 올렸습니다(앱은 빈 문자열을 "없음"으로 방어 중). **`kakao-rest-key-switch` 회신**: 변수명 **`KAKAO_REST_KEY`** 확정 — `env.dart`·`app-web.yml`·`.env.example`을 이미 바꿨고 등록만 기다립니다. **`INTERNAL_SHARED_SECRET`은 앱에 주지 말라고** 못 박았습니다(웹 번들에 들어가면 누구나 내부 엔드포인트를 부릅니다). 테스트 65건.
>
> **수정 기록 (2026-09-05 ㊸, 앱)** — **화면이 샘플 대신 실제 API를 보게 바꿨고, 샘플 모드를 스위치로 만들었습니다.** 화면은 이제 `JournalRepository` 하나만 보고 `Sample`도 `ApiClient`도 직접 보지 않습니다 — 구현이 `Api…`/`Sample…` 둘이라 화면 코드를 고치지 않고 갈아끼웁니다. 프로바이더는 `core/providers.dart`가 단일 출처이고, 로딩·빈 상태·오류는 `AsyncView`가 한 곳에서 처리합니다. **계약 v1.4 §2-8도 이번에 반영했습니다** — `Trend`에 `userAvgGap`·`tagGaps`가 실려 오고, 위젯이 들고 있던 `TagGap`을 모델로 옮겼습니다. S05-1 삭제는 실제로 `DELETE`를 치고 연쇄 무효화 때문에 기록·발견·추세를 함께 무효화하며, S06 탈퇴는 **서버가 지운 뒤에** 기기를 비웁니다(순서를 뒤집으면 토큰이 없어져 다시 삭제할 수도 없습니다). S02는 세션 시작·이어하기·폴링·종료까지 실제로 돌고 **EVI 연결만 남았습니다.** **샘플 모드가 있는 이유는 Hume 과금입니다** — EVI는 통화 시간만큼 돈이 나가서 화면을 볼 때마다 실제 세션을 열 수 없습니다. 샘플 모드는 백엔드도 Hume도 타지 않고, `startSession()`이 **가짜 토큰**을 줘서 실수로 통화가 열릴 수 없으며, `live()`가 대본대로 12초 뒤 위기 신호를 올려 **S07을 실제 위기 발화 없이** 확인시켜 줍니다. 켜는 방법은 `--dart-define=SAMPLE_DATA=true` · 주소에 **`?sample=1`**(배포 URL에서도 됩니다) · S06 설정 셋입니다. **웹 릴리스에서만 터지는 함정 하나를 브라우저 렌더로 잡았습니다** — 인터페이스 쪽 optional 파라미터에 기본값을 두고 구현에서만 채우면 dart2js가 `null`을 넘겨 `NoSuchMethodError`로 죽는데, **VM 테스트는 통과합니다.** nullable + `?? const PageQuery()`로 고치고 회귀 테스트를 넣었습니다. 같은 확인에서 **오류를 빈 상태로 바꿔 말하던 홈 화면 버그**도 잡았습니다 — "아직 발견한 것이 없습니다"는 사실 주장이라 못 불러온 것을 그렇게 적으면 거짓이 됩니다. 테스트 48건.
>
> **수정 기록 (2026-09-05 ㊷, 앱)** — **S04 트렌드의 폭 예외를 걷어냈습니다. 이제 모든 화면이 420입니다.** 예외의 근거가 "30일에서 점 마커가 살아남는 폭"이었는데, **모바일에서는 성립하지 않는다**는 지적을 받고 재봤습니다 — `< 600`은 전폭이라 넓힐 여지가 없고(폰 390에서 30일이 하루당 11px, 임계 14px), **매일 쓰는 환경이 그쪽입니다.** 게다가 **90일은 900에서도 임계를 못 넘습니다**(9.3px). 세 범위 중 하나만, 한 환경에서만 듣는 처방이었고 대가는 화면마다 폭이 튀는 것이었습니다. **밀도는 폭이 아니라 차트가 풀도록 고쳤습니다** — 점이 촘촘하면 모든 날에 찍지 않고 **양 끝과 음영 구간의 날**에만 찍습니다(반지름 4→3). 이건 모바일에서도 그대로 들어서 "언제 벌어졌는지"가 폰에서도 점으로 보입니다. 샘플 트렌드도 **30일치로 늘렸습니다** — 범위가 30일인데 표본이 14일이면 밀도를 확인할 수 없었습니다. §2-1은 "예외 두 곳"에서 **한 곳(데모 모드)** 으로 줄었고, 걷어낸 근거를 숫자 표와 함께 남겼습니다. 테스트 38건(밀도 3건 신설, 폭 테스트는 트렌드도 420인지 보도록 반전).
>
> **수정 기록 (2026-09-05 ㉟, AI)** — **AI서버 엔드포인트 4종이 전부 섰습니다. 테스트 161건 통과. 통합 준비 목표였던 9/6보다 하루 빠릅니다.** `POST /chat/completions`(Hume CLM, SSE) · `POST /internal/observations` · `POST /internal/summaries` · `GET /healthz`. **백엔드가 오늘 바로 붙일 수 있습니다** — 통합 순서 1번(`GET /internal/sessions/{id}`)과 2번(`POST /internal/turns`) 둘 다 AI 쪽 준비가 끝났고, **Hume 키가 없어도 이 둘은 검증됩니다.** 세션 조회는 계약대로 **200 `ended`와 404를 401로 바꾸고**, 캐시 미스 + 5xx는 fail-closed, 캐시 히트는 백엔드가 죽어도 통과합니다. 턴 적재는 5xx·타임아웃만 3회 지수 백오프이고 **4xx는 재시도하지 않으며, 재시도 페이로드의 `occurredAt`이 최초 시도와 문자열까지 같습니다**(중복 판별 가드의 전제, 테스트로 고정). **설계 제약을 테스트로 내렸습니다** — `app/rules/`에 네트워크·LLM import가 생기면 AST 검사가 깨지고, 분석·응답 payload에 프로소디가 섞이면 **보내기 전에** 예외가 납니다(FR-025·TC-24), 로그는 화이트리스트라 `transcript=`를 실수로 넘겨도 버려집니다(FR-092). **`request/ai/hume-account-setup.md` 회신(✅)** — 계정 소유·결제를 받고, **490분 기준 총액이 Creator $27.30 < Starter $34.50이라 승인 상한인 Creator가 가장 싼 선택**입니다(분기점 약 97분). 콘솔 실측 4건은 가입이 사람 손을 타는 단계라 계정 생성 후 회신에 덧붙입니다. 공유 시크릿은 **비밀번호 관리자 공유 항목**을 제안했습니다(카톡·메일 금지). **아직 검증하지 못한 것 3가지**: Hume 실제 연결(Config 미생성) · Anthropic 실제 호출(키 미발급) · 백엔드 실제 연결(터널 전). **코드는 서 있지만 한 번도 진짜 모델을 부른 적이 없습니다.**
>
> **수정 기록 (2026-09-04 ㉞, 앱)** — **하단 탭을 떠 있는 반투명 알약 칩 + 아이콘으로 바꿨습니다** (design-system §7 결정 24). 「카드를 쓰지 않는다」(§1)의 **유일한 예외**이고, 제가 반대 의견을 냈던 항목입니다 — 앱 담당 결정으로 채택했으니 반대 근거와 함께 문서에 남겼습니다. **예외를 좁게 두는 제약 넷**: ① 면색은 `bg` 78%이고 `lift`를 쓰지 않습니다(§4 유지) ② 아이콘은 탭에서만 씁니다 ③ 블러는 탭이 있는 4개 화면에만 — **S02에는 칩이 없어 움직이는 링 위에서 `BackdropFilter`가 돌지 않습니다** ④ §2-1로 900까지 넓어지는 S04에서도 칩은 셸 폭에 고정합니다(내비게이션이 화면마다 늘어나면 같은 메뉴로 안 읽힙니다). **칩이 콘텐츠 위에 뜨는 문제**는 셸이 칩 높이를 `MediaQuery` 아래 여백으로 넘기고 각 화면이 **스크롤 뷰 안쪽** 여백으로 받아 해결했습니다 — 스크롤 중에는 글자가 칩 밑을 지나가고(반투명이 보이는 이유) 멈추면 가리지 않습니다. 홈은 아래가 스크롤이 아니라 CTA라 실제 여백을 줬습니다. 다크·라이트 × 모바일·데스크톱으로 렌더해 확인했고 테스트 35건 유지입니다. `app/design/`의 시안 PNG는 이 결정 이전이라 그 사실을 README에 적었습니다 — **화면 기준은 이제 배포된 앱**입니다.
>
> **수정 기록 (2026-09-04 ㉝, 앱)** — **카카오 웹 로그인 요청에 회신했습니다(✅ `response/backend/kakao-web-login.md`). 인가 코드 방식 확정입니다.** 문서 인용이 아니라 **`kakao_flutter_sdk` 2.0.1을 내려받아 소스를 읽어** 닫았고, 결론이 백엔드 추정보다 한 단계 강했습니다 — 웹에서는 `loginWithKakaoAccount()`·`issueAccessToken()` 등이 **전부 `KakaoClientException(notSupported)`를 던지고**, `authorize()`마저 `window.location.href`로 페이지를 넘긴 뒤 **빈 문자열**을 돌려줍니다. 그래서 **웹에는 카카오 SDK를 넣지 않습니다**(하는 일이 URL 조립뿐). 계약 §2-1 개정안(`kakaoAuthCode`·`redirectUri`)은 그대로 확정하고 PKCE는 넣지 않기로 했습니다. Redirect URI 등록값·로컬 포트 3000·해시 라우팅 유지·동의항목(회원번호만)도 확정 — **이름을 쓰는 화면이 없다는 것을 11개 화면에서 확인**했습니다. **백엔드에 확인 2건을 남겼습니다** — ① SDK가 인가 URL의 `client_id`에 **JS 키**를 넣는데 교환은 REST 키로 하실 계획이라, 혼용이 되는지(안 되면 인가 URL도 REST 키로 만들고 **앱 키 변수명이 `KAKAO_REST_KEY`로 바뀝니다**) ② **F10-03 unlink를 앱이 할 수 없게 됐습니다** — 인가 코드 방식에서는 카카오 토큰이 백엔드에만 남아 "앱이 본인 토큰으로 끊는다"는 전제가 깨집니다. Admin 키 없이 **사용자 액세스 토큰으로 서버가 끊는 안**을 적었습니다(리프레시 토큰 보관 여부는 백엔드 결정). `app/README.md`에 카카오 절을 만들고, **CORS 회신의 "포트 고정 불필요"와 카카오의 "포트 고정 필요"가 충돌해 보이는 이유**도 함께 적었습니다.
>
> **수정 기록 (2026-09-04 ㉜, 앱)** — **CORS 회신(`response/app/cors-origin.md`)을 앱에 반영했습니다.** 앱이 지킬 것 셋 중 **하나는 코드 제약**입니다 — 백엔드가 로컬용으로 `http://localhost:*` 와일드카드를 열었으므로 **`withCredentials`를 켜면 로컬 개발이 전부 CORS로 막힙니다**(와일드카드와 `Allow-Credentials`는 배타). dio 기본값이 `false`라 지금은 맞고, 나중에 "편의상" 켜지 않도록 `core/network/api_client.dart` 주석에 근거와 함께 못 박았습니다. 같은 주석에 **CORS 차단이 앱에서는 그냥 네트워크 오류로 보인다**는 진단 메모도 남겼습니다 — 배포 URL에서 모든 호출이 한꺼번에 실패하면 오프라인이 아니라 오리진 목록을 먼저 의심합니다. `--web-port` 고정은 권고대로 하지 않습니다. `app/README.md`에 「백엔드에 붙이기」 절을 만들고 낡은 문장을 고쳤습니다 — 저장소 공개로 무의미해진 "비공개 Pages는 유료" 경고, "아직 URL이 없다", 계약 v1.4에서 이미 해결된 `tagGaps`가 ⏳로 남아 있던 행.
>
> **수정 기록 (2026-09-05 ㊶, 백엔드)** — **백엔드 Phase 1~7이 전부 끝났습니다. 남은 것은 계정 두 개(Supabase·Render)와 다른 파트의 값·회신뿐입니다.** **배포 이미지로 확인했습니다** — Docker 빌드 → `PORT` 주입 기동 → DB 연결 → **TC-14 데모 모드**(`UPDATE` 한 줄, 재배포 없음). **배포 당일에야 드러났을 결함 둘을 미리 잡았습니다** — ① `gradlew`에 **실행 비트가 없어** 리눅스 빌드가 permission denied로 죽었습니다 ② 문서의 환경변수 목록에 **계약 v1.6에서 없어진 `KAKAO_APP_ID`가 남아 있고 필수인 `KAKAO_REST_API_KEY`가 빠져** 있었습니다(그대로 등록하면 부팅에서 죽습니다). **로그 감사 통과** — 코드·실행 로그 양쪽에서 `transcript` 0건 · `sessionId` 0건 · 음성 파일 코드 0건. **막혀 있는 것을 `backend/docs/blocked.md`에 "무엇이 막는가"로 묶어 뒀습니다** — **Hume 계정 하나가 4건을 막고 있는 게 지금 가장 큰 병목**입니다(플랜은 Creator로 결정됐고 가입만 남았습니다). **앱에 두 가지가 급합니다** — repo variable 이름 확정(`KAKAO_REST_KEY`)과 `chat-group-id.md` 회신. **AI에는** Hume 계정 · `ANTHROPIC_API_KEY` · 통합 결함 2건 회신입니다.
>
> **수정 기록 (2026-09-05 ㊵, 백엔드)** — **막혀 있던 것 2건을 닫았습니다. 상대가 없어서가 아니라 도구가 없어서였습니다.** ① **AI가 제안한 재시도 시나리오를 실제로 돌렸습니다** — 고장 주입 프록시로 `POST /internal/turns`를 **백엔드에 저장시킨 뒤 503으로 바꿔** 돌려줬더니, AI서버가 턴마다 **4회(최초 1 + 재시도 3)** 보내고 `turn_post_failed retries: 3`으로 포기했습니다. **같은 턴을 총 8회 받고도 백엔드 행은 2개, `TURN_INDEX_COLLISION` 0건** — `occurredAt` 판별이 실제로 동작합니다. **백엔드에 고장 스위치는 넣지 않았습니다**(배포까지 따라갑니다). ② **관찰·요약의 성공 경로를 처음 돌렸습니다** — 대역 AI서버로 `회의`(갭 1.50 × 3)와 `산책`(갭 0.10 × 3)을 넣으니 **`회의`만 관찰이 되고 `산책`은 침묵**했습니다. `evidence`는 `3 · 1.50 · 0.80 · 1.88`, 근거 대화 3건이 연결되고 `GET /api/observations`에 그대로 나옵니다. **AI로 나간 본문도 눈으로 확인했습니다** — 숫자와 태그 5개뿐이고 원본 대화·turn ID가 없습니다(계약 §3-3). **그 과정에서 결함 하나를 잡았습니다** — **`gapAvg`가 `0.80000000000000000000`으로 나가고 있었습니다**(계약 §1-1은 소수 2자리). `POST /api/session/{id}/end`와 `GET /api/sessions` 두 곳이었고 고쳤습니다. **앱에서 그 값을 그대로 그리셨으면 화면에 20자리가 떴을 겁니다.** 테스트 84건 통과.
>
> **수정 기록 (2026-09-05 ㊴, 백엔드)** — **통합 1·2번을 돌렸습니다. AI 필독 — 결함 2건이 나왔습니다**(`request/ai/integration-round-1.md` ⏳). **터널 대신 양쪽을 제 로컬에서 띄웠습니다** — `ai-server/`가 같은 저장소에 있어 그대로 실행했고, 터널을 열 필요도 양쪽이 동시에 붙어 있을 필요도 없었습니다. **잘 된 것** — Hume 흉내로 CLM 요청을 쏘니 **AI → 백엔드 세션 조회 200 → CLM 200 SSE → 턴 2건 적재**까지 한 번에 갔습니다. **프로소디 규칙이 실제로 `voiceValence −0.81`을 계산**했고, `ANTHROPIC_API_KEY`가 없어 분석은 실패했는데 **`textValence: null` · `gap: null`로 degraded**되는 것까지 계약 §1-3·TC-06 그대로였습니다. **백엔드를 내리고 CLM을 쏴도 200으로 끝났습니다** — fire-and-forget이 성립합니다(F5-04). 백엔드 → AI의 `/internal/summaries`·`/internal/observations`도 본문을 받아들이고 키가 없어서만 실패했습니다. **⚠️ 결함 ①** — **fail-closed 경로가 500으로 터집니다.** 모르는 세션·`ended`·조회 실패에서 401을 돌려줘야 하는데 `error_log()` 인자 충돌로 예외가 납니다(`main.py:90`). **정상 경로에서는 절대 안 밟히는 코드라** 통합 전까지 안 보였습니다. **⚠️ 결함 ②** — **AI 로그에 `sessionId`가 평문으로 7건** 남습니다. 계약 §1-1이 "CLM 인증 수단이라 비밀과 동급, 로그에 남기지 않는다"로 못 박은 값입니다. 백엔드는 `sessionRef`(SHA-256 앞 8자)를 쓰고 같은 시간대 로그에 **0건**입니다. **제 쪽에서 고친 것 1건** — AI서버 포트를 8000으로 잘못 알고 있었습니다(`.env.example`의 `AI_PORT=8100`이 맞습니다). **`ANTHROPIC_API_KEY` 한 줄이면 나머지가 다 열립니다.**
>
> **수정 기록 (2026-09-05 ㊳, 백엔드)** — **Phase 6 구현 완료 — 백엔드 기능 Phase가 전부 끝났습니다(1~6).** `DELETE /api/sessions/{id}`(대화 삭제 + **관찰 연쇄 무효화** + baseline 재계산) · `DELETE /api/account`(10테이블 삭제 + 카카오 unlink). **테스트 83건 통과.** **앱에게** — ① **삭제 응답의 `removedObservationIds`·`recalculatedObservationIds`가 실제로 채워집니다.** 받으시면 관찰 목록 캐시를 버려 주세요 — 근거를 잃은 관찰이 화면에 남으면 그 순간 "근거 없는 문장"이 됩니다(FR-081) ② **탈퇴는 본문 없이도 204입니다.** 카카오 인가 코드를 본문에 실어 주시면 서버가 연결 해제까지 하고, 없으면 데이터만 지웁니다(계약 §2-3 v1.6) ③ **탈퇴 화면이 붙으면 실계정 unlink 왕복을 한 번 봐야 합니다** — 탈퇴는 되돌릴 수 없어 제 쪽에서 임의로 돌려볼 수 없습니다. **AI에게** — 회신 잘 받았습니다. **`INTERNAL_SHARED_SECRET`은 만들어서 팀장님이 직접 전달합니다.** 터널은 **Phase 7(배포) 전에** 열어 통합 1·2번을 한 번에 맞추기로 했습니다 — 제안하신 **재시도 시나리오**(응답만 버려 재시도 유도)도 그때 같이 돌리겠습니다.
>
> **수정 기록 (2026-09-05 ㊲, 백엔드)** — **Phase 5 구현 완료 — 앱 화면 5개(S03·S03-1·S04·S05·S05-1)가 실제 데이터로 그려집니다.** `GET /api/observations` · `/observations/{id}/evidence` · `POST /observations/{id}/feedback` · `GET /api/trend` · `GET /api/sessions` · `/sessions/{id}` 전부 계약대로 나갑니다. **앱이 알아두실 것 넷** — ① **관찰이 없으면 빈 배열입니다**(`total: 0`). 오류가 아니고, "아직 없어요" 안내는 앱 몫입니다 ② **기록 없는 날은 `points`에서 아예 빠집니다** — 0으로 채우지 않으니 선을 끊어 그리시면 됩니다(TC-18) ③ **기록 목록(`GET /api/sessions`)은 끝난 세션만** 내려갑니다. 진행 중인 대화는 `GET /api/me`의 `openSession`으로만 보입니다 ④ **`range`를 생략하면 `30d`이고, 모르는 값을 보내도 400이 아니라 `30d`로 돕니다.** **`tagGaps`는 "3회 이상이지만 1.5배 미만"인 태그도 포함합니다** — 요청서(`tag-gap-endpoint.md`)에서 말씀하신 대로 비교 대상이 있어야 "왜 이게 발견인가"가 설명되니까요. 3회 미만만 서버가 거릅니다. **`userAvgGap`은 관찰 `evidence.userAvgGap`과 같은 값**이라 화면 두 곳의 숫자가 어긋나지 않습니다. **관찰은 AI서버가 붙어야 생깁니다** — 지금은 0건이라 S03이 빈 상태로 뜨는 것이 정상입니다.
>
> **수정 기록 (2026-09-05 ㊱, 백엔드)** — **Phase 4 구현 완료 (패턴 배치·관찰 생성). AI 필독.** 태그별 집계 → **코드 판정**(3회 이상 AND 평균의 1.5배 이상) → **통과한 것만** `POST /internal/observations`로 문장화 요청 → 저장. **보내는 것은 숫자와 태그뿐입니다** — 원본 대화·발화·turn ID를 넣지 않습니다(계약 §3-3 그대로). **판정은 코드가 하고 AI서버는 문장만 씁니다**(FR-054). **AI서버가 아직 없어 지금은 관찰이 0건이고 그게 설계대로입니다** — 템플릿 폴백을 두지 않아 문장화가 실패하면 관찰을 만들지 않고 다음 주기에 다시 시도합니다. **`GET /internal/sessions`의 `recentObservations`도 이제 실제 값이 나갑니다**(빈 배열이 아니라). **spec에 규정이 없던 것 하나를 정했습니다** — **같은 태그로 이미 살아 있는 관찰이 있으면 새로 만들지 않습니다.** 집계가 전체 기간이라 한 번 조건을 넘긴 태그는 세션마다 계속 넘기는데, 그대로 두면 **발견 화면(S03)이 같은 문장으로 도배되고 문장화 호출도 매번 나갑니다.** 관찰을 "시점의 발견"으로 두고 숫자가 자라도 갱신하지 않습니다 — **이견 있으면 말씀해 주세요. 도그푸딩 뒤에 다시 정할 항목으로 열어뒀습니다.**
>
> **수정 기록 (2026-09-05 ㉟, 백엔드)** — **계약 v1.6 로그인을 실계정으로 끝까지 확인했습니다(앱 참고).** 인가 코드를 그대로 `POST /api/auth/kakao`에 넣어 **서버가 교환 → JWT → `GET /api/me`**까지 돌았습니다. **주목할 결과 하나** — **로그인 방식이 바뀌었는데도 `profileId`가 그대로입니다.** 어제 액세스 토큰 방식으로 만든 계정에 오늘 인가 코드 방식으로 들어가 같은 `98c2fece…`가 나왔고 `isNewUser: false`였습니다. 저장하는 값이 **카카오 회원번호 하나**라서입니다 — **도그푸딩 중에 방식을 바꿔도 기존 데이터가 끊기지 않습니다.** **같은 코드를 두 번 쓰면 401**인 것도 확인했습니다(1회용) — 적어주신 "주소창의 `?code=`를 지워야 한다"가 바로 이 경우입니다. 로그에 **인가 코드·REST 키·클라이언트 시크릿 0건**입니다. **`redirectUri` 화이트리스트도 실서버에서 확인**했습니다: 미등록 주소는 **카카오를 부르기 전에 400**입니다.
>
> **수정 기록 (2026-09-05 ㉞, 백엔드)** — **Phase 3 구현 완료. AI·앱 모두 해당됩니다.** **AI에게** — `POST /internal/turns`가 돌아갑니다. **그쪽 고정 픽스처 3종(`ai-server/eval/fixtures/internal/`)을 그대로 `curl`로 보내 실서버에서 확인**했습니다: `turns.user`·`turns.assistant`·`turns.user.degraded` 전부 **202**, 저장·암호화·null 처리까지 맞습니다. **재시도와 충돌도 실제로 갈렸습니다** — 같은 페이로드 두 번은 행 1개, 같은 인덱스 + 다른 `occurredAt`은 행 2개 + `ops_error_log`. **적재 응답시간 p95 20ms**입니다. **한 가지 바뀐 동작**: **깨진 JSON이 이제 500이 아니라 400**입니다 — 5xx를 3회 재시도하시는데 본문이 깨진 요청은 재시도해도 같은 결과라서요. 4xx이니 재시도하지 마시고 넘어가시면 됩니다. **`POST /internal/summaries` 호출도 붙였습니다** — 지금은 AI서버가 없어 실패 경로로 `summary: null`입니다. **터널이 열리면 그 자리에서 붙습니다.** **앱에게** — **`GET /api/session/{id}/live`가 돌아갑니다(S02를 끝내실 수 있습니다).** 비데모 계정은 `turns`가 **빈 배열**이고(`null` 아닙니다) `crisisDetected`는 **데모 여부와 무관하게 정상 값**입니다 — 실서버에서 확인했습니다(TC-26). `sinceTurnIndex`도 동작합니다. **여전히 막혀 있는 건 Hume 키뿐**이라 `session/start`만 503입니다.
>
> **수정 기록 (2026-09-05 ㉝, 백엔드)** — **루트 `CLAUDE.md`의 세션 시작 명령을 고쳤습니다. 세 역할 모두 해당됩니다.** `grep -L "✅"`가 **본문에 ✅가 한 번만 나와도 그 요청을 목록에서 빼버립니다.** 어제 넣은 규칙인데 오늘 실제로 걸렸습니다 — 백엔드가 새 요청서에서 다른 문서를 가리키며 "(✅ 회신 완료)"라고 적었더니 **자기가 방금 올린 미회신 요청이 안 보였습니다.** 배너 문자열로 찾도록 바꿨습니다.
>
> ```sh
> git pull && grep -L "상태: ✅" docs/request/{내 역할}/*.md
> ```
>
> **배너 형식(`> **상태: ⏳ 회신 대기**`)이 그대로면 아무것도 안 하셔도 됩니다** — 명령만 바뀌었습니다. 놓친 요청이 있었는지 이 명령으로 한 번 확인해 주세요.
>
> **수정 기록 (2026-09-05 ㉜, 백엔드)** — **계약 v1.6 — 카카오 로그인이 인가 코드 방식으로 바뀝니다. 앱 필독.** 앱 회신(`response/backend/kakao-web-login.md`)이 SDK 2.0.1 소스로 확인해 준 대로 **웹에서는 앱이 액세스 토큰을 받을 수 없습니다.** ① **§2-1 요청이 `kakaoAccessToken` → `kakaoAuthCode` + `redirectUri`로 교체**됐습니다(응답은 그대로). **이 회차의 유일한 필드 삭제입니다.** ② **§2-3(탈퇴)에 선택 본문 신설** — 카카오 unlink용 인가 코드. 없어도 204이고 unlink 실패는 오류로 올리지 않습니다 ③ **앱이 물은 두 가지에 답했습니다**(`request/app/kakao-rest-key-switch.md` ⏳) — **`client_id` 혼용은 시험할 필요가 없습니다.** 권하신 대로 인가 URL도 REST 키로 가면 되고, **그 경로는 제가 어제 실계정으로 이미 확인한 것**입니다. 그래서 **repo variable이 `KAKAO_JS_KEY` → `KAKAO_REST_KEY`로 바뀝니다**(이름만 확정해 주시면 등록은 제가 합니다). **`unlink`는 사용자 액세스 토큰으로 됩니다** — 어드민 키 방식은 `target_id`로 남을 끊는 경로라, **어드민 키를 서버에 두지 않는다는 결정을 지키면서** 백엔드가 할 수 있습니다. ④ **탈퇴 unlink 담당이 앱 → 백엔드로 넘어왔습니다**(spec F10-03 개정). **리프레시 토큰은 보관하지 않습니다** — 보관하면 전 사용자의 2개월짜리 카카오 자격증명을 우리 DB가 들게 되어 "저장하는 것은 회원번호 하나"(PRD §5.1·F10-04)가 길어집니다. 대신 **탈퇴 시점에 앱이 인가를 한 번 더 통과**해 코드를 넘깁니다. ⑤ **`app_id` 대조를 걷어냈습니다**(spec F1-01 개정) — 우리 REST 키로 교환한 토큰은 정의상 우리 앱 것이라, **Phase 1의 유일한 미검증 항목이 사라졌습니다.** 대신 **`redirectUri` 화이트리스트 검증**이 들어갔습니다(미등록이면 카카오를 부르기 전에 400).
>
> **수정 기록 (2026-09-04 ㉛, 백엔드)** — **Phase 2 구현 완료. 앱·AI 모두 해당됩니다.** 세션 시작·종료·이어하기 · **`GET /internal/sessions/{id}`**(AI의 CLM 인증) · F2-06 스케줄러 정리 · `GET /api/me`의 **`openSession`**. **테스트 36건 통과.** **AI에게** — 합의한 통합 순서의 **첫 칸(`GET /internal/sessions`)이 열렸습니다.** 시크릿 없으면 401, 없는 세션 404, `ended`도 200, `lastTurnIndex`는 적재된 최대 인덱스(지금은 턴이 없어 0), `recentObservations`는 빈 배열(Phase 4). **다만 `INTERNAL_SHARED_SECRET` 값을 아직 아무도 만들지 않았습니다** — 백엔드가 만들어 드릴 테니 **전달 경로만 알려주세요**(`request/ai/hume-account-setup.md`에 덧붙였습니다). **앱에게** — `POST /api/session/start`는 **다 만들어졌지만 Hume 키가 자리표시라 지금은 503으로 떨어집니다**(정상 동작이고, 그때 세션을 만들지 않는 것까지 확인했습니다). `GET /api/me`의 `openSession`은 이제 실제 값이 나갑니다. **구현하다 문서 결함 2건을 잡았습니다.** ① **`resumableUntil`을 `ended_at + 30분`으로 계산할 수 없습니다** — `openSession`은 아직 열려 있는 세션이라 `ended_at`이 NULL이고, `started_at` 기준으로 바꾸면 이번엔 **TC-22가 깨집니다**(2분 말하고 앱이 죽은 뒤 5분 지나 이어하기하면 벽시계로 7분이 흘러 잔여 시간이 0이 됩니다). 앱이 죽으면 종료 신호가 오지 않으므로 **서버가 아는 마지막 활동은 마지막 턴의 `occurred_at` 하나뿐**이고, `usedSec`·이어하기 창·F2-06 정리를 전부 그 기준으로 고쳤습니다 — 계약 §2-2의 "중단 후 30분"과도 이쪽이 맞습니다. **계약 필드는 바뀌지 않았습니다** — 대신 **spec F2-06의 처리 조건("시작 후 30분" → "마지막 발화 후 30분")을 정정**했습니다. ② **`resumedChatGroupId`를 채우는 주체가 어느 문서에도 없습니다** — EVI의 `chat_group_id`는 앱이 Hume과 직접 붙어 받는 값이라 백엔드가 알 수 없습니다. 지금은 항상 null이고 **이어하기는 되지만 이전 맥락 복원만 안 됩니다.** `request/app/chat-group-id.md`(⏳)로 물었습니다 — **F2-07이 P1이라 급하지 않습니다.** **`POST /internal/summaries` 호출은 Phase 3으로 미뤘습니다** — 보낼 발화도, 그 본문을 평문으로 읽는 복호화 변환기도 Phase 3에서 생깁니다. 계약 §2-5가 `summary: null`을 허용합니다.
>
> **수정 기록 (2026-09-04 ㉚, 앱)** — **데스크톱에서 컨텐츠 영역이 검정으로 보이던 문제.** 색을 재보니 시안 PNG와 앱 렌더가 **둘 다 `#0D0D0C`로 같았습니다** — 바뀐 건 색이 아니라 면적이었습니다. 시안은 390 폭 아트보드가 캔버스 회색 위에 얹혀 있어 같은 값이 "짙은 회색"으로 읽혔는데, 앱은 컨텐츠와 바깥이 같은 `bg`라 1280px 전체가 한 덩어리가 되어 near-black이 그냥 검정으로 읽혔습니다. 토큰 `desk`를 신설해(다크 `#050504` · 라이트 `#E7E3DA`) `>= 600`에서 바깥에만 쓰고, 컨텐츠 폭 **바깥**에 `line` 1px 세로선을 둬 경계를 만들었습니다(안쪽 테두리로 넣으면 셸 폭이 418로 줄어 §2가 다시 깨집니다). 다크·라이트 모두 데스크톱에서 렌더해 확인했습니다. 문서는 §4 토큰 표에 `desk` 행과 사용 규칙, §2 그림·표의 "배경"을 `desk`로 정정했습니다.
>
> **수정 기록 (2026-09-04 ㉚, 백엔드)** — **카카오 로그인을 실계정으로 끝까지 돌렸습니다(앱 참고).** 인가 코드 → 토큰 교환 → `POST /api/auth/kakao` → `GET /api/me` 왕복 확인, **TC-01(재로그인 동일 `profileId`)** 은 같은 토큰 1회 + 리프레시로 받은 새 토큰 1회로 확인했습니다. **계약 §2-1·§2-2 응답이 문서 그대로 나옵니다.** 로그에 카카오 토큰·회원번호 **0건**, `account`에 든 컬럼은 `id`·`kakao_id`·`created_at` **셋뿐**이라 동의항목이 회원번호만인 것이 실제로 확인됐고, `account.id`와 `profile.id`가 달라 **식별자 분리(PRD §5.1)가 실물로 성립**합니다. **그래서 웹에서 `OAuthToken`이 나오는 흐름이면 앱은 토큰만 넘기면 끝입니다 — 백엔드는 손댈 게 없습니다.** 남은 미검증은 **`app_id` 대조 음성 케이스**(다른 카카오 앱이 있어야 확인 가능) 하나뿐입니다. `request/app/kakao-web-login.md`에 덧붙였습니다.
>
> **수정 기록 (2026-09-04 ㉙, 백엔드)** — **카카오 콘솔에 Redirect URI를 등록했습니다. 앱에 통보 1건 + 확인 요청 1건**(`request/app/kakao-web-login.md`에 덧붙였습니다 — 같은 카카오 스레드라 문서를 새로 만들지 않았습니다). ① **로컬 개발 포트를 3000으로 정했습니다** — 등록값은 `https://hackathon-yaho.github.io/emotion/`와 `http://localhost:3000/`이고, 앱은 `flutter run -d chrome --web-port=3000`으로 고정해 주셔야 합니다. **카카오 콘솔은 CORS와 달리 와일드카드를 안 받습니다.** 포트가 다르면 **로그인만 실패하고 나머지 API는 멀쩡히 되므로**(CORS는 `localhost:*`로 열려 있습니다) 원인을 찾기 어려운 종류입니다. **경로를 앱 루트로 잡은 이유** — 하위 경로로 받으면 GitHub Pages에 그 파일이 없어 404이고(SPA 폴백 `404.html` 없음), 지금 앱이 `usePathUrlStrategy`를 부르지 않아 **해시 라우팅**이라 루트로 받아도 `?code=...#/경로`가 깨지지 않습니다. **path 전략으로 바꾸실 계획이면 미리 알려주세요.** ② **웹은 인가 코드까지만 오는 것으로 보입니다 — 사실이면 계약 §2-1이 바뀝니다.** 카카오 Flutter 문서가 웹 `authorize()`에 대해 "인가 코드만 발급되며, 서비스 서버에서 REST API로 토큰을 발급받아야 한다"고 씁니다. 개정안(`kakaoAuthCode` + `redirectUri`, 응답은 그대로)을 요청서에 미리 적어뒀고 **확인은 앱이 SDK를 한 번 붙여보시는 게 가장 빠릅니다.** 코드 방식이면 **REST 키가 서버에만 남고 `app_id` 대조는 불필요해집니다.** **어느 쪽이든 ①의 등록값은 그대로 유효합니다** — 두 흐름 모두 브라우저가 돌아가는 곳은 앱입니다. ③ **`KAKAO_JS_KEY` 전달 경로는 아직 답을 못 받았습니다** — repo variable이 비어 있어 배포 빌드에 키가 안 들어갑니다.
>
> **수정 기록 (2026-09-04 ㉘, 백엔드)** — **백엔드 Phase 1을 구현해 올렸습니다. `backend/`에 코드가 생겼습니다.** Java 21 · Spring Boot 3.4.5 · Gradle · docker-compose Postgres 16 · **DB 11테이블 전부**. 구현한 API 3개 — **`POST /api/auth/kakao`**(카카오 토큰 검증 + `app_id` 대조 + 자체 JWT 발급) · **`GET /api/me`** · **`GET /api/health`**(DB에 실제로 닿습니다 — cron 킵얼라이브와 Supabase 유휴 방지를 겸합니다). `DELETE /api/account`는 계약대로 204를 주지만 **라우트와 인증만** 걸려 있습니다(삭제 로직은 Phase 6). **테스트 18건 통과** — 단위 10 + 통합 8. 통합은 compose DB에 붙어 **`migration.sql`을 그대로 적용**하므로 스키마도 배포와 같은 파일로 검증됩니다. **앱에 직접 관계있는 것 둘** — ① **CORS가 열려 있습니다.** `https://hackathon-yaho.github.io`와 `http://localhost:*`(포트 고정 불필요) 프리플라이트가 인증 없이 200으로 통과하는 것을 실제 서버로 확인했습니다. 백엔드 주소만 받으면 바로 붙이실 수 있습니다. ② **`GET /api/me`의 `openSession`은 아직 항상 `null`입니다** — Phase 2에서 채웁니다. 나머지 필드(`profileId`·`joinedAt`·`sessionCount`·`thresholdMode`·`demoMode`)는 계약 §2-2 그대로 나갑니다. **막혀 있는 것 하나** — 실제 카카오 로그인은 **`KAKAO_JS_KEY`(앱)와 `KAKAO_APP_ID`(백엔드) 값이 들어가야** 끝단까지 돕니다. 그래서 **TC-01만 미검증**입니다 — `request/app/kakao-web-login.md`(⏳).
>
> **수정 기록 (2026-09-04 ㉗, 백엔드)** — **Phase 1 착수 전 점검. 앱·AI에 각각 확인할 것이 있습니다.** 문서에 근거 없이 비어 있던 6건을 결정했습니다 — **스케줄러 5분**(`fixedDelay`) · **시크릿 등급 분리**(`TRANSCRIPT_ENC_KEY`만 오프라인 사본, 나머지 둘은 재발급으로 끝) · **카카오 동의항목은 회원번호만** · **탈퇴 시 카카오 unlink를 한다** · **테스트는 통합까지, 실DB는 기존 compose 재사용** · **Supabase는 배포 시점 생성 + Session pooler(5432)**. **앱에 3건**(회신 대기 중인 `request/app/kakao-web-login.md` 스레드에 덧붙였습니다 — 카카오 문서를 또 만들지 않았습니다) — ① **카카오 JS 키를 repo variable에 넣어야 합니다.** 콘솔은 백엔드가 만들어 소유하지만 **등록할 주소(사이트 도메인·`redirect_uri`·로컬 포트)는 앱 배포 주소**라 확정이 필요하고, **카카오 콘솔은 CORS와 달리 와일드카드를 안 받아 로컬 포트를 하나 고정해야 합니다.** **이게 Phase 1의 F1-01을 막습니다.** ② **동의항목을 회원번호만 열었습니다**(통보) — `account`에 담을 컬럼이 없어 받아도 버리고, PRD §5.1 "식별자 분리"가 콘솔 설정으로도 증명됩니다. **화면에 사용자 이름을 쓸 계획이 있으면 지금 말씀해 주세요 — 나중에 열면 기존 사용자는 재동의입니다.** ③ **탈퇴 시 카카오 연결 해제를 앱이 맡아 주세요** — 백엔드 204를 받은 **뒤에** SDK로 `unlink`. 서버에서 하려면 **모든 사용자를 조작할 수 있는 Admin 키**가 필요해서 서버에 두지 않기로 했고, 앱은 본인 토큰으로 자기 것만 끊습니다. **AI에 1건**(`request/ai/hume-account-setup.md`에 덧붙임) — **넘겨주실 키가 두 개입니다.** 단기 토큰 발급에 **API 키와 Secret 키가 짝으로** 필요한데 환경변수 목록에 `HUME_API_KEY`만 있었습니다. Phase 2에서 막힙니다.
>
> **수정 기록 (2026-09-04 ㉖, 백엔드)** — **소셜 로그인 방식을 점검하고 보안 단계 하나를 spec에 넣었습니다. 앱에 확인 요청 1건(⏳).** 계약 §2-1·spec F1-01이 정한 것은 **액세스 토큰 전달 방식**(앱이 카카오 SDK로 로그인 → 토큰을 백엔드로 → 백엔드가 검증 → 자체 JWT를 응답 바디로)이고, 이 선택은 배포 구조가 강제합니다 — **앱이 GitHub Pages, 백엔드가 Render라 도메인이 완전히 달라서 쿠키 기반(리다이렉트 + HttpOnly 쿠키) 방식은 서드파티 쿠키로 막힙니다.** 쿠키를 쓰면 `Allow-Credentials`를 켜야 하고, 그러면 방금 합의한 `localhost:*` 와일드카드 오리진도 못 씁니다. **① spec F1-01에 `app_id` 대조를 명시했습니다** — ③이 "카카오 API로 토큰 검증" 한 줄이라 그대로 짜면 `/v2/user/me`만 부르고 끝나는데, 그러면 **아무 카카오 앱에서 발급된 토큰이든 통과합니다**(카카오 앱은 누구나 만들 수 있습니다). `GET /v1/user/access_token_info`로 `app_id`를 대조하는 단계를 넣었고, **빠뜨려도 정상 로그인은 멀쩡히 돼서 테스트로는 안 잡히는 종류**라 완료 기준에도 넣었습니다. **② `account.kakao_sub` → `kakao_id`** — 저장하는 값은 `/v2/user/me`의 `id`(회원번호)인데 `sub`은 OIDC 클레임 이름이라 이름과 값이 달랐습니다. **백엔드 내부 컬럼이라 앱·AI 영향은 없고**, 코드가 없는 지금이 고치는 비용이 0인 유일한 시점입니다. **③ 앱에 요청 — `request/app/kakao-web-login.md`(⏳).** 웹은 리다이렉트로 **인가 코드**를 받는 흐름이 기본이라, spec의 "SDK가 반환한 액세스 토큰"이 웹에서 그대로 성립하는지 확인이 필요합니다. **인가 코드 방식이면 계약 §2-1을 개정합니다** — 백엔드는 어느 쪽이든 만들 수 있고, 오히려 REST 키가 서버에만 있는 코드 교환 방식이 낫습니다. 다만 정하기 전에 짜면 한쪽을 버리게 됩니다.
>
> **수정 기록 (2026-09-04 ㉕, 백엔드)** — **앱 요청 `cors-origin.md`에 회신했습니다(✅).** 허용 오리진은 **환경변수 목록**(`CORS_ALLOWED_ORIGINS`)으로 두어 커스텀 도메인이 붙어도 재배포가 필요 없고, 로컬은 `allowedOriginPatterns`로 `http://localhost:*`를 그대로 열어 **앱이 `--web-port`를 고정할 필요가 없습니다.** **회신하다 백엔드 결함을 하나 잡았습니다** — `phase-1`의 JWT 필터를 "인증 제외 경로 2개 외 전부 필수"로 적어둬서, 그대로 구현하면 **`OPTIONS` 프리플라이트가 401로 막힙니다**(프리플라이트에는 `Authorization`이 안 실립니다). 앱이 요청서에 그 조건을 명시해 준 덕에 구현 전에 걸렸습니다. **계약서는 고치지 않았습니다** — CORS는 배포 설정이지 인터페이스 필드가 아니라, 특정 오리진을 계약에 적으면 필드가 하나도 안 바뀌는데 버전을 올려야 합니다. **그리고 Hume 계정 소유를 확정했습니다. AI 쪽에서 만들어 소유하고 결제도 그 계정 위에서 합니다.** AI가 알려준 문제(`config_id`가 워크스페이스에 묶여 **Config 계정과 결제 계정이 갈리면 결제한 쿼터가 적용되지 않고, 대화는 붙었다가 5분 뒤 끊기며 원인이 코드로 안 보이는** 것)에 대한 팀 결정입니다. **테스트와 대시보드 확인이 전부 AI 쪽**이라 계정을 그쪽에 두는 편이 Config 설정마다 생기는 왕복이 없습니다. **백엔드는 API 키와 `config_id`만 받아 환경변수에 넣습니다**(저장소에 넣지 않습니다). **예산 상한은 Creator 플랜**이며 상한이지 지정이 아닙니다 — **동시 접속만 보면 Starter(5)로 3인이 이미 충족**이라, 상위 플랜의 값어치는 **포함 분수·초과 단가**에 있습니다(필요량 약 490분, PRD §11). PRD §2.5가 3자 요금 정보를 확정값으로 쓰지 못하게 해서 **콘솔 확인 4항목**(포함 분수·초과 단가 / 동시 접속 / 신규 $20 크레딧이 EVI에 적용되는지 / 결제 수단 등록 위치)과 함께 **`request/ai/hume-account-setup.md`(⏳)** 로 보냈습니다. **이 건이 백엔드 Phase 2의 `session/start` 실동작과 9/6 EVI 통합을 막습니다** — Free는 월 5분·동시 접속 1이라 **하드 컷 7분짜리 세션 하나가 월 할당량보다 깁니다.**
>
> **수정 기록 (2026-09-04 ㉔, 앱)** — **웹에서 화면마다 폭이 달랐던 문제를 고쳤습니다.** 넓은 화면 폭 규칙(§2)이 셸 위젯 안에만 있어서 **탭 4개 화면만 420으로 고정되고 셸 밖 화면(S00·S02·S02-1·S03-1·S05-1·S06)은 데스크톱에서 전폭으로 늘어났습니다.** 폭 결정을 `AppFrame` 하나로 모아 `MaterialApp.router`의 `builder`에 한 번만 걸었고, 1280 폭에서 12개 라우트를 다시 렌더해 열 화면이 모두 같은 폭(내용 372px)임을 픽셀로 확인했습니다. 같은 작업에서 **§2-1 예외 두 곳을 실제로 구현했습니다** — S04 트렌드는 900까지 확장, 데모 모드는 `>= 600`에서 오른쪽 패널. 테스트 **35건**(폭 5건 신설). 문서는 design-system §2에 구현 지점과 값을 적고, §2 표의 낡은 탭 라벨("홈·트렌드")을 §7-6 확정 라벨로 맞췄습니다. 백엔드에 **CORS 허용 오리진 요청**(`docs/request/backend/cors-origin.md`)을 올렸습니다 — 배포 주소가 정해져서 나온 항목입니다.
>
> **수정 기록 (2026-09-04 ㉓, 백엔드)** — **AI 회신 2건을 백엔드 문서에 반영했습니다**(`response/backend/`). ① **`turnIndex` 채번 ✅** — AI서버 카운터가 `lastTurnIndex`로 시드돼 **0에서 시작하는 경로가 설계상 없고**, 이어하기는 유휴 60초로 감지해 재조회합니다. 요청했던 `occurredAt` 확인이 **계약 v1.5 §3-2**에 규칙으로 명시되어 백엔드 중복 판별 가드의 전제가 확정됐습니다 — 그래서 **`TURN_INDEX_COLLISION`은 이제 "정상 동작"이 아니라 "버그 신호"**입니다(한 건이라도 보이면 AI에 알립니다). ② **통합 경로 ✅** — 터널 방식·순서 동의, **AI서버 9/6 준비**, 고정 픽스처(`ai-server/eval/fixtures/internal/`)로 터널 전 `curl` 선검증. `roadmap.md`에 순서 표를 넣었습니다. **⚠️ 그리고 결제 블로커가 하나 왔습니다 — Hume Config를 만드는 계정과 결제하는 계정이 같아야 합니다.** 갈리면 결제한 쿼터가 그 `config_id`에 적용되지 않아 **대화는 붙고 5분 뒤 끊기며 원인이 코드로 안 보입니다.** 팀 공용 계정 하나를 정해야 하고, 정하는 쪽은 결제 주체(백엔드)입니다. **아울러 이 배너의 번호가 충돌해 바로잡았습니다** — 앱·백엔드가 각자 ⑲를 썼습니다(병렬 작업). 본문은 그대로 두고 **커밋 순서대로 번호만** 고쳤습니다(앱 ⑲→㉑, AI ㉑→㉒). **배너를 쓰기 전에 `git pull`로 맨 위 번호를 확인해 주세요.**
>
> **수정 기록 (2026-09-04 ㉒, AI)** — **AI에게 온 요청 2건에 회신했습니다(✅ `turn-index-numbering.md` · `integration-test-path.md`).** ① **`occurredAt` 규칙을 계약 §3-2에 명시하고 v1.5로 올렸습니다** — 백엔드의 중복 판별 가드가 이 필드 하나에 걸려 있는데 계약에는 예시만 있고 규칙이 없었습니다. **발화 시각 · 밀리초 정밀도 · 재시도 시 동일 문자열**입니다(필드 추가·삭제 없음). ② **`turnIndex`가 0에서 시작하는 경로를 설계에서 없앴습니다** — 카운터를 세션 캐시 엔트리 안에 두어 캐시가 죽으면 카운터도 죽고, 캐시가 죽으면 재조회가 돌아 **재시작이 곧 재시드**가 됩니다. 이어하기는 AI서버에 보이지 않으므로 **유휴 60초로 감지**해 `lastTurnIndex`를 다시 읽습니다. ③ **통합 테스트는 양쪽 터널에 동의했고 AI서버 준비 목표는 9/6입니다.** 붙이는 순서는 `GET /internal/sessions` 먼저. **`ai-server/eval/fixtures/internal/`에 내부 API 고정 JSON 10개를 올렸으니 터널 전에 `curl`로 먼저 맞춰보실 수 있습니다.** ④ **세션 조회 실패 시 fail-open을 철회하고 fail-closed로 확정**했습니다(백엔드 판단 수용 — fail-open은 "백엔드 장애 시간 = 인증 무방비 시간"). 개발 편의용 우회 스위치는 만들지 않고 통합 기간에는 타임아웃만 `.env`로 올립니다. ⑤ **`prompts/summary.system.md` 신설** — 계약 §3-5 요약 경로. **요약에 갭이 새지 않는 것이 핵심**이라(S02가 숨기는 수치를 문장이 흘리면 안 됩니다) 숫자·감정 단정을 사후 검사로 폐기합니다. ⑥ **⚠️ Hume 무료 티어로는 통합 테스트조차 안 됩니다** — Free는 **월 5분 · 동시 접속 1**이라 하드 컷 7분짜리 세션 하나가 월 할당량보다 깁니다. **동시 접속 수는 어느 문서에도 없던 항목이라 PRD §11·§14-3에 추가**했습니다(3인 동시 도그푸딩은 Free에서 성립하지 않습니다). 그리고 **Config를 만드는 계정과 결제하는 계정이 같아야 합니다** — `config_id`는 워크스페이스에 묶여서, 갈리면 결제한 쿼터가 적용되지 않고 **대화는 붙었다가 5분 뒤 끊깁니다.**
>
> **수정 기록 (2026-09-04 ㉑, 앱)** — **화면 11개를 전부 Flutter 위젯으로 옮겼습니다** (S00~S07). 공용 위젯 신설 — 두 선 그래프·날짜 축·범례, 이야기별 갭 막대, S02의 어긋난 두 링, 「두 겹」 텍스트, 카카오 규격 버튼, 확인 시트. `analyze` 0 이슈 · 테스트 **30건** · 웹 빌드 성공, 12개 라우트를 헤드리스로 렌더해 눈으로 확인했습니다. **옮기는 과정에서 계약 위반 하나를 잡았습니다** — 계약서 §1-3은 기록이 없는 날을 `points`에서 생략하는데, 제 페인터가 배열 순서로 x를 놓아 **9/4·9/9·9/10에서 선이 끊기지 않고 이어졌습니다.** x를 날짜 기준으로 바꾸고 하루 차이가 아니면 끊도록 고쳤으며, `ChartAxis` 테스트 7건으로 못 박았습니다. 그 외 S02 정렬, S05-1에서 AI 발화의 `stretch` Row가 뒤 항목을 밀어내던 문제, S07이 장식 링을 중복으로 그리던 문제를 고쳤습니다. 화면은 아직 `core/fixtures/sample_data.dart`의 샘플로 그립니다 — **발표 근거로 쓰지 않습니다**(PRD §12).
>
> **수정 기록 (2026-09-04 ⑳, 백엔드)** — **루트 `CLAUDE.md`에 세 역할 공통 규칙이 하나 생겼습니다. 앱·AI 모두 해당됩니다.** 작업 시작 시 `git pull && grep -L "✅" docs/request/{내 역할}/*.md`로 **나에게 온 미회신 요청부터 셉니다.** 그리고 **미회신 건수를 어느 문서에도 적어두지 않습니다** — 백엔드가 자기 문서에 "4건 전부 회신 완료"라고 적어둔 탓에 5번째 요청(`tag-gap-endpoint.md`)을 못 볼 뻔했습니다. 표가 틀린 게 아니라 쓴 순간에는 맞았고 그 뒤에 낡은 것이라, **규칙을 더하지 않고 베낀 숫자를 지우는 쪽**으로 고쳤습니다. 아울러 백엔드 내부 결함 하나를 계약 변경 없이 닫았습니다 — `/internal/turns`의 `unique` 위반을 `occurred_at`으로 판별해 **재시도(무시)와 충돌(`max+1`로 저장 + `ops_error_log`)을 가릅니다.** 이어하기 후 인덱스가 리셋돼도 **턴이 유실되지 않고 로그에 남습니다.** AI 회신을 기다리지 않으며, `request/ai/turn-index-numbering.md`는 블로커에서 확인 요청으로 내려갔습니다 — 다만 **`occurredAt`이 발화 시각이고 재시도에서 불변인지**가 그 판별의 유일한 전제라 AI 확인이 필요합니다.
> **수정 기록 (2026-09-04 ⑲, 백엔드)** — **`api-contract.md`를 v1.4로 개정했습니다. 앱·AI 모두 영향이 있습니다(필드 삭제·개명은 없습니다).** 백엔드 계획 문서를 루트 스펙과 전수 대조하다 나온 공백·결함을 메웠습니다. ① **`request/backend/tag-gap-endpoint.md` 회신(✅)** — `GET /api/trend`에 **`tagGaps`·`userAvgGap`** 추가. 다섯 결정 모두 앱 제안대로(§2-8, `range` 종속 · 3회 미만 필터는 서버 · 상위 7개). ② **`highlights`(F9-02)의 판정 기준이 없었습니다** — 임계값은 PRD §14-5로 반드시 한 번 바뀌는데 적용값을 저장하지 않아, 바꾸는 순간 **과거 트렌드의 음영이 소급 변경**됩니다. `voice_session.gap_threshold` 스냅샷을 신설했습니다(앱이 받는 응답 형태는 그대로). **spec 총괄표 F9-02 담당을 `C` → `A/C`로 정정** — 서버가 계산해 내려주는데 담당에 백엔드가 없었습니다. ③ **`turnIndex` 채번 규칙을 §3-2에 명시하고 §3-4에 `lastTurnIndex`를 추가 — AI 확인 요청 1건 신설(⏳ `request/ai/turn-index-numbering.md`).** 이어하기는 같은 `sessionId`를 쓰는데 재연결 후 인덱스를 리셋하면 백엔드의 중복 방어에 걸려 **이후 턴이 오류 없이 202로 버려집니다.** ④ **F3-04에 `avg_gap IS NOT NULL` 가드** — 5세션 내내 분석이 실패하면 평균이 없는 채로 `personal`로 전환됐습니다. **`session_count` 증가를 F3-05(P1·스코프 컷 7번)에서 분리** — 자르면 P0인 F3-04와 TC-07이 같이 죽었습니다. ⑤ **`profile.demo_mode` 신설** — F11-01 플래그의 실체가 어느 문서에도 없었습니다(환경변수 대신 컬럼으로: 심사 당일 재배포 없이 `UPDATE` 한 줄). ⑥ 계약 §2-5·spec §5 시나리오의 "배치 큐 적재"를 실제 방식(`pattern_processed_at`)으로 정정, PRD §14 이슈 3·9 갱신.
>
>
> **수정 기록 (2026-09-04 ⑱, 앱)** — **팀원이 디자인을 볼 경로를 만들었습니다.** 그동안 화면을 아티팩트 링크로만 안내했는데, **아티팩트는 기본 비공개이고 내보내기가 선언된 캔버스는 공유해도 조직 내부로 제한**되므로 팀원 전원이 열 수 있는 경로가 아니었습니다. `app/design/*.dc.html`도 캔버스 런타임 안에서만 렌더되는 소스라 직접 열리지 않습니다. 그래서 **`app/design/export/`를 신설**하고 화면 ID 기반 파일 이름 규칙(`S00-onboarding.png` …)과 갱신 규칙을 정했습니다. `design-system.md`에 **§8 "화면을 어디서 보나"** 를 넣고, `app/design/README.md`의 "재생성하세요" 안내도 앱 담당 전용으로 범위를 좁혔습니다 — 그 명령은 Claude Code의 `/design` 스킬을 요구해 다른 역할에게는 성립하지 않습니다. **이미지 파일은 캔버스 툴바의 Export로 뽑아야 하므로 앱 담당이 채워 넣습니다.**
>
> **수정 기록 (2026-09-04 ⑰, 앱)** — 백엔드 회신(계약 v1.3)을 앱 코드에 반영했습니다. `SessionStart`에 **`humeConfigId`(null 불가)·`livePollIntervalSec`** 추가, `SessionResume`도 `humeConfigId` 필수로. **`.env`의 `HUME_CONFIG_ID`와 폴백 코드를 제거**했습니다 — 계약에 필드가 생겼으므로 임시 경로를 남길 이유가 없습니다. §2-13 `GET /api/session/{id}/live`용 `LiveSignal`·`LiveTurn` 모델 신설. 테스트로 못 박은 두 가지: **`turns: []`는 "볼 권한이 없다"이지 "값이 없다"가 아니고**(§1-3의 null과 뜻이 다름), **S07은 `crisisDetected`의 false→true 전이에서 한 번만** 뜹니다. 테스트 23건 통과.
>
> **수정 기록 (2026-09-04 ⑯, 앱)** — 골격 누락 점검 결과 반영. **F1-05 "동의 없이 대화 화면 진입 불가"가 코드에서 지켜지지 않고 있었습니다** — 웹은 주소창에 `/conversation`을 치면 온보딩을 건너뛸 수 있었고, 라우터 가드(`AppSession`)를 넣어 막았습니다(브라우저에서 우회 시도 후 S00에 머무는 것 확인). 인증 응답 모델·목록 페이징 봉투(§1-4)도 빠져 있어 추가했습니다. **spec 총괄표의 F1-04·F10-03 담당을 `A` → `A/C`로 정정** — 확인 다이얼로그가 앱 작업인데 담당에 앱이 없었습니다. `design-system.md` §7에 결정 19~22 추가(`sessionCount`·`createdAt`·`endReason` 표시, 페이징은 "더 보기" 없이 이어 불러오기). **`request/backend/tag-gap-endpoint.md` 신설(⏳)** — F9-03 이야기별 갭의 출력이 계약 어디에도 없습니다.
>
> **수정 기록 (2026-09-04 ⑮, 백엔드)** — 백엔드 실행 문서(`backend/docs/`)를 전면 보강하면서 **`spec.md` §6-1의 공백 3건을 메웠습니다(v1.2)** — ① `turn_log`에 **`role`·`occurred_at`** 추가(계약 §2-10·§3-2가 요구하는데 컬럼 목록에 없었습니다) ② `voice_session`에 **`pattern_processed_at`** 추가(F7-01 배치 방식 확정) ③ **F10-01 처리에 `crisis_event` 삭제 추가** — 이게 없으면 **세션 삭제가 FK 위반으로 실패**합니다. F7-01 처리도 "큐 적재"에서 실제 방식(스케줄러 스캔)으로 구체화했습니다. **DB 스키마의 단일 출처는 `backend/docs/data-model.md`**로 신설했고, 앱·AI가 볼 필드 정의는 종전대로 `api-contract.md`입니다.
>
> **수정 기록 (2026-09-04 ⑭, 백엔드)** — **일정의 위상이 바뀌었습니다. 앱·AI 모두 읽어주세요.** ① **PRD §12를 v1.1로 개정** — **9/10 도그푸딩 시작이 "일정 상수"에서 "팀 내부 목표"로 내려갔고, 고정 마감은 9/20 제출 하나만 남았습니다.** ② **배포는 세 파트(앱·백엔드·AI)의 기능 확인이 끝난 뒤에 합니다**(팀 결정). 도그푸딩이 웹 링크 공유로 진행되므로 **배포 시점 = 도그푸딩 시작 시점**입니다. ③ 밀릴 때의 대가를 §12에 함께 적었습니다 — 로그 기간이 짧아지면 FR-051 조건에 미달해 **S03 발견 화면이 빈 채로 심사받을 수 있습니다**(조건 미달 시 억지 관찰을 만들지 않는 것이 설계이므로, FR-052). 각자 요청서에 "9/10 도그푸딩" 전제로 우선순위를 적어두신 부분이 있다면 기준이 달라졌습니다. ④ 백엔드는 스택을 확정했고(Java 21·Boot 3.4.5·Gradle·`com.hackathonyaho.voicejournal`·Render), **AI에 요청 1건 신설(⏳)** — `request/ai/integration-test-path.md`: 양쪽이 로컬이면 AI서버가 백엔드에 도달할 수 없어 통합 검증이 성립하지 않습니다.
>
> **수정 기록 (2026-09-03 ⑬, 백엔드)** — 백엔드가 받은 요청 4건에 회신하며 **`api-contract.md`를 v1.3으로 개정**했습니다. `sessionId`를 UUIDv4로 교체(CLM 인증 수단이라 엔트로피 확보), `humeConfigId`·`livePollIntervalSec` 필드 추가, **`GET /api/session/{id}/live` 신설**(대화 중 위기·갭 신호 폴링), **`GET /internal/sessions/{id}` 신설**(CLM 인증 겸 세션 컨텍스트 조회, fail-closed), **`POST /internal/summaries` 신설**(요약 동기 생성), `/internal/turns` 재시도 1회→3회. `spec.md`는 v1.1로 F2-01·F2-03·F2-05·F4-04·F5-04·F11-01 개정 + TC-26·27 신설. 회신 4건은 `response/app/`·`response/ai/`, 요청 배너는 전부 ✅. 상세 결정 근거(왜 fail-closed인지, 왜 폴링인지 등)는 각 회신 문서 참조. **`backend/docs/`(백엔드 실행 계획 문서, 2026-09-03 신설)의 결정 로그·Phase 문서도 함께 갱신했습니다.**
>
> **수정 기록 (2026-09-03 ⑫, 앱)** — **`app/`에 Flutter 골격을 올렸습니다.** 디자인 토큰(§4)을 `ThemeExtension`으로, 타입 스케일(§3)을 `AppType`으로 옮기고, spec §4의 화면 11개를 go_router 라우트로 두었습니다(하단 탭 4개는 셸 라우트). 계약서 §2의 응답을 그대로 옮긴 모델 4종을 만들며 **null 규칙(§1-3)과 "assistant 턴은 valence·gap이 전부 null"을 타입에 반영**했고, `ApiClient`는 §1-2 오류 봉투를 해석하고 **대화 중 401이 대화를 끊지 않도록** 콜백으로만 알립니다. `flutter analyze` 0 이슈 · 테스트 10건 통과 · `flutter build web --release` 성공. 화면 본문은 아직 자리표시이며 `app/design/`의 확정 디자인을 옮기는 것이 다음 작업입니다.
>
> **수정 기록 (2026-09-03 ⑪, 앱)** — **P1 기능 4건도 그렸습니다** (기능으로 들어가는 것은 디자인에도 있어야 한다는 판단). F2-07 이어하기(S01 제안 블록 + 남은 시간 승계 + 30분 창 경과), F7-08 피드백 선택 후 상태(취소 없음), F9-03 이야기별 갭 막대(내 평균 실선 · ×1.5 점선 · 조건을 넘은 막대만 밝게), 기간 7·30·90일(90일은 마커 숨김). `design-system.md` §7에 결정 14~17 추가. 90일 샘플의 앞 76일은 시연용 결정적 난수이며 캔버스 주석에 표기.
>
> **수정 기록 (2026-09-03 ⑩, 앱)** — 디자인 커버리지 점검 결과 반영. **spec §4 하단 탭 라벨을 "오늘 · 발견 · 추세 · 기록"으로 변경**(화면 ID·화면명은 그대로). `design-system.md` §7에 결정 8건 추가(탭 라벨 · S02-1 `gapAvg` 미표시 · S05 `tags`·`gapAvg` 표시 · S03-1 valence 둘 다 표시 · 로딩 스켈레톤 규칙 · 빈 상태 규칙 · 파괴적 동작에 `care` 금지 · 문구 톤 "-습니다"), §7-1 문구를 새 톤으로 다시 쓰고 탈퇴·삭제 확인 문구 추가. 화면은 11개 모두 있었고 **빠진 것은 상태였다** — 빈 상태(도그푸딩 첫 며칠이 전부 빈 화면), 오류 3분기, 확인 시트 2종을 캔버스에 추가.
>
> **수정 기록 (2026-09-03 ⑨, 앱)** — **디자인 방향을 「밤의 활자 + 두 겹」으로 교체하고 화면 8개를 다시 그렸습니다.** ⑧의 "차분한 밤"은 카드 나열·파스텔 강조·원형 블롭이라 생성물 티가 난다는 판단으로 버렸습니다. 새 골격은 **카드를 쓰지 않고**(헤어라인·여백으로만 구획) 관찰 문장을 **한글 명조**로 읽게 만들며, **「두 겹」(어긋난 두 겹)은 갭이 실제 주제인 두 곳** — S02 대화의 두 링, S04 추세의 제목 — 에만 씁니다. `design-system.md` §1·§3·§4·§5를 새 팔레트로 갱신했고, **배경이 남색에서 따뜻한 검정으로 바뀌어 계열 색을 다시 측정**했습니다(첫 팔레트는 명도·채도 밴드를 벗어남 → `#5A8FC9`/`#C25A38`로 교체, 두 테마 전 항목 PASS). 작업 파일은 `app/design/`에 있고 캔버스 시드 산출물(2.4MB)은 `.gitignore`에 넣었습니다.
>
> **수정 기록 (2026-09-03 ⑧, 앱)** — **`01-product/design-system.md` 신설.** 앱 디자인 방향을 **"차분한 밤"**으로 확정하고, 레이아웃 규약(390 기준 · 셸 최대 420 · 분기점 600 하나)·사이징 스케일·**다크/라이트 토큰 양쪽**·S02 대화 화면의 금지 사항·나머지 UX 결정 5건을 담았습니다. **S04 두 선 그래프 색은 색각이상·대비 검증기를 돌려 두 테마 모두 전 항목 PASS**를 확인했습니다. 문서화하며 발견한 것 하나 — **웹 배포라 데스크톱에서 `tel:`이 동작하지 않아** spec F4-04의 "109 통화 연결 버튼"만으로는 시연 환경에서 무의미합니다(번호 표시 + 복사 병행으로 결정, §7).
>
> **수정 기록 (2026-09-03 ⑦, AI)** — `request/ai/clm-turn-pipeline-review.md` **회신 완료(✅)**. PRD §9.1을 **분석 호출(전사만 입력) + 응답 호출** 구조로 개정하고 **FR-025(프로소디 미노출, P0)** 를 신설했습니다. 태그·위기 LLM 판정도 분석 호출로 옮겨 **응답 스트림에 메타 태그가 없습니다.** 상세 설계는 신설한 **`02-architecture/ai-pipeline.md`** 가 단일 출처입니다. spec F3-02·F3-06·F4-01·F4-03·F6-01·F6-02·TC-06 개정, **TC-24·25 신설**, api-contract **v1.2**(필드 변경 없음, §1-3 `voiceValence` null 사유 보완 포함). PRD §14의 미결 1(매핑표)·2(위기 키워드)·AI 스택(Python/FastAPI)을 해소했고(`ai-server/rules/`·`prompts/`), 임계값(5)은 결정 절차만 확정. spec F6-01의 동의어 병합 예시(`미팅`→`회의`)는 FR-043·§1.4와 충돌해 삭제했습니다. **백엔드에 요청 2건 신설(⏳)**: `request/backend/session-context-lookup.md`(AI서버가 임계값·모드를 알 경로 + CLM 인증), `session-summary-endpoint.md`(F2-05 요약 생성 경로).
>
> **수정 기록 (2026-09-03 ⑥)** — ⑤의 타깃 서술을 정리했습니다. **웹과 모바일 앱을 모두 지원 타깃으로 두고(우선순위 없음), 배포·제출·시연은 웹 URL 하나로 합니다.** 프레임워크(Flutter)와 실측 결과는 그대로이며, 제품 정의는 **"모바일 서비스"로 유지**합니다(⑤에서 "모바일 우선 웹 서비스"로 바꿨던 것을 되돌림). 웹 배포라 도그푸딩이 링크 공유로 끝나므로 스토어 심사·기기 등록 문제가 없습니다.
>
> **수정 기록 (2026-09-03 ⑤)** — **앱 프레임워크 확정: Flutter.** 결정 근거·실측 결과·전환 조건은 **PRD 신설 §14-4**에 있습니다(빌드·렌더링·EVI 세션 수립은 실측, **마이크 음성 왕복은 미검증**). 이에 맞춰 PRD(헤더·§1.1·§2.1·§6·§6.1·§14), spec(헤더·F2-02), 루트 `README.md`, `CLAUDE.md`, `app/README.md`를 갱신했습니다.
>
> **수정 기록 (2026-09-03 ④)** — `request/backend/hume-config-id.md` 신설(⏳). Hume 공식 Flutter 예제를 **웹으로 빌드해 실행**하며 실제 EVI handshake를 확인한 결과, `access_token`은 계약서 §2-4와 1:1로 맞지만 **`config_id`에 해당하는 필드가 계약에 없습니다.** CLM을 우리 AI서버로 붙이는 순간부터 연결이 성립하지 않습니다. 앱 프레임워크는 **Flutter(웹 우선)** 로 확정되어 가는 중이며, 확정 시 PRD·spec의 "모바일 서비스"·"RN 또는 Flutter" 서술을 함께 고칩니다.
>
> **수정 기록 (2026-09-03 ③)** — `request/backend/live-turn-signal.md` 신설 — 대화 중 턴 신호 조회 엔드포인트 요청(⏳). **P0 두 개(S07 위기 안내 시트, S02 데모 모드 수치 노출)가 앱 쪽에서 데이터 경로 없이 남아 있는 것**을 발견했습니다. 위기 감지·갭·valence는 `/internal/turns`로 백엔드에만 도달하고 계약서 §2에 앱이 읽을 필드가 없습니다. 회신 전까지 두 화면은 UI만 만들고 트리거 연결은 보류합니다.
>
> **수정 기록 (2026-09-03 ②)** — 루트에 `CLAUDE.md` 추가(에이전트 세션 시작 시 읽는 안내판 — 읽을 문서·절대 규칙·경계 감시·협업 규칙·현재 상태). `request/ai/clm-turn-pipeline-review.md` 신설 — PRD §9.1 실시간 턴 처리의 순환 의존·채널 독립성 문제 검토 요청(⏳). **회신 전에는 §9.1·FR-021 구현에 착수하지 않습니다.**
>
> **수정 기록 (2026-09-03 ①)** — 저장소 신설. `hackathon-yaho/idea`의 `emotion/` 폴더에 있던 PRD·기능명세서·API 계약서를 **내용 변경 없이** 해빙(2026-finance-ai-challenge) 문서 구조로 옮겼습니다. 폴더가 나뉘면서 끊어지는 상대 링크 7개만 보정했습니다(prd 2 · spec 4 · api-contract 1).
> PRD가 참조하는 내부 문서 중 **`decisions.md`, `voice-emotion-stack-options.md`, `selected-topic.md` 3개는 아직 이 저장소에 없습니다.** 원본을 가진 사람이 `00-context/`에 올려 주세요 — PRD의 링크가 그 위치를 가리키고 있습니다.

원티드 AI Championship 2026 제출 프로젝트. 앱 1 · 백엔드 1 · AI 개발자 1, 총 3인 협업 기준으로 구성된 문서입니다. 제출 마감 **2026-09-20**, 팀 실사용(도그푸딩) 시작 **2026-09-10**.

## 이 문서를 처음 보신다면

1. **`00-context/prd.md`** 를 먼저 읽으세요. 이 프로젝트의 단일 진실 공급원(source of truth)입니다. 기능 단위의 더 상세한 명세가 필요하면 **`00-context/spec.md`**(기능명세서, F1~F11 · 55개 기능)를 참조하세요. 둘이 다르면 **PRD가 우선**합니다.
2. 인터페이스는 **`02-architecture/api-contract.md`** 가 단일 출처입니다. 필드 수준에서 PRD·spec과 다르면 **계약서가 우선**합니다.
3. 본인 역할의 요청 폴더를 확인하세요 → `request/app/` / `request/backend/` / `request/ai/` (내가 보낸 요청의 처리 결과는 `response/{내 역할}/`)

## 핵심 설계 원칙 (전원 필독)

> **절대 감정을 단정하지 않는다.** 이 제품의 주장은 "같은 말을 다르게 하면 갭이 갈린다"뿐이다(PRD §1.4). 갭 = |텍스트 valence − 음성 valence|이고, 임계 초과 시 AI가 **되묻는다** — 단정하지 않는다(FR-022·030). 갭 수치는 기록·트렌드 화면에서만 보이고 대화 화면에는 나오지 않는다(FR-031).

> **판정은 코드, 표현은 LLM.** 음성 valence 환산(FR-020), 갭 계산(FR-022), 관찰 생성 조건(FR-051)은 전부 결정적 규칙이다. LLM은 대화 응답·태그·관찰 **문장**만 만든다(FR-054). 태그는 발화 원문에 등장한 표현만 허용하고 코드가 대조한다(FR-043). 관찰에는 evidence가 반드시 붙고, 문장 ↔ 숫자 불일치는 **0건**이어야 한다(§1.4).

> **오탐 정책은 기능군마다 반대다.** 갭·패턴은 **정밀도 우선**(애매하면 침묵), 위기 감지는 **재현율 우선**(애매하면 감지). 위기 감지는 규칙 OR LLM이며, LLM이 죽어도 키워드 규칙은 단독으로 동작한다(FR-032). 감지 시 109를 안내하되 **대화를 끊지 않는다**(FR-033). F4는 어떤 스코프 컷에서도 자르지 않는다(spec §11).

> **음성 원본은 우리 서버에 오지 않는다.** 앱↔Hume 구간에만 존재하며, AI서버는 텍스트와 점수만 받는다(FR-041·§5.1). 발화 텍스트는 암호화 저장, 카카오 식별자는 감정 데이터와 분리, 탈퇴는 유예 없이 전량 삭제.

이 원칙이 무너지면 심사 방어 논리 전체가 흔들립니다. 앱은 대화 화면에 갭 수치를 그리지 않고, 백엔드는 관찰 판정에 임의 로직을 섞지 않으며, AI는 프롬프트로 판정을 대신하지 않습니다.

## 문서 지도

| 경로 | 내용 | 주 대상 |
| --- | --- | --- |
| `00-context/prd.md` | PRD 전문 (source of truth) — 문제·근거 출처·FR 56개·데이터 모델·AI 파이프라인·비용·데모 | 전원 |
| `00-context/spec.md` | 기능명세서 (F1~F11, 55개) — 상세 명세·화면 11개·시나리오·TC 23개·**스코프 컷 순서** | 전원 |
| `00-context/03-emotional-voice-journal.html` | 초기 리서치 리포트 (2026-08-30). 인용 수치는 PRD §2.5의 "1차 출처 미확인" 항목 | 전원 (참고용) |
| `00-context/decisions.md` | ⚠️ **누락** — 주제 선정 과정과 15개 결정의 근거 | 전원 |
| `00-context/voice-emotion-stack-options.md` | ⚠️ **누락** — 스택 4조합 비교 (Hume EVI+CLM 채택 근거) | AI, BE |
| `00-context/selected-topic.md` | ⚠️ **누락** — 주제 확정 기획 | 전원 |
| `01-product/design-system.md` | **디자인 시스템** — 방향(차분한 밤) · 레이아웃 규약 · 사이징 스케일 · 다크/라이트 토큰 · 트렌드 그래프 색(검증 완료) · S02 금지 사항 · UX 결정 | 앱 |
| `02-architecture/api-contract.md` | **인터페이스 단일 출처** — 앱↔백엔드 12개 · 내부 2개 · Hume CLM 외부 계약 · 오류 규약 · null 규칙 | 앱 ↔ BE ↔ AI |
| `02-architecture/ai-pipeline.md` | **AI서버 설계 단일 출처** — 실시간 턴 처리, LLM 경계, 매핑표·위기 규칙·태깅, 배치, 실패 처리, 평가, 레포 구조 | AI, BE |
| `05-planning/git-branching.md` | Git 운영 규칙 (main 직접 커밋) — 해빙에서 가져옴 | 전원 |
| `request/app/*` | 앱 담당자에게 요청할 사항 문서 | 앱 |
| `request/backend/*` | 백엔드 담당자에게 요청할 사항 문서 | BE |
| `request/ai/*` | AI 담당자에게 요청할 사항 문서 | AI |
| `response/app/*` | 앱이 보낸 요청에 대한 회신 | 앱 |
| `response/backend/*` | 백엔드가 보낸 요청에 대한 회신 | BE |
| `response/ai/*` | AI 담당자가 보낸 요청에 대한 회신 | AI |
| `../ai-server/` (프롬프트 파일) | **프롬프트 전문의 단일 출처.** 문서에는 조항 요지만 둔다 (PRD §9.3) | AI |

`01-product/`는 디자인 시스템 문서와 함께 만들었습니다(2026-09-03). 해빙에 있던 `03-infra-ops/`, `04-testing/` 폴더는 지금은 비어 있어 만들지 않았습니다. 해당 성격의 문서(페르소나 분리, 배포·프라이버시, 테스트·데모)가 PRD·spec에서 갈라져 나올 때 같은 번호로 만듭니다.

## 문서 갱신 규칙

- `request/app/*`, `request/backend/*`, `request/ai/*`는 **다른 역할에게 보내는 요청 문서**만 담습니다(요청 하나당 파일 하나). 갭 계산 규칙, API 스키마 같은 실제 스펙 본문을 복사해 넣지 마세요 — 스펙이 바뀌면 두 곳을 다 고쳐야 하고, 결국 어긋납니다. 스펙이 바뀌면 `00-context/`, `02-architecture/` 원본만 고치고 request는 그대로 둡니다.
- **요청 문서 맨 위에 회신 상태 배너를 답니다** (⏳ 회신 대기 / ✅ 회신 완료 / ⚠️ 일부 회신). 요청만 쌓이면 어느 게 아직 살아 있는 요청인지 알 수 없습니다. 회신이 와도 **요청 본문은 고치지 않고** 배너만 바꿉니다 — 당시 무엇을 물었는지가 기록이기 때문입니다. 형식은 `request/app/README.md` "회신 상태 표시 규칙" 참조. (해빙은 이 규칙을 나중에 추가했는데 그 전에 "요청이 반영됐는지 확인할 방법이 없다"는 문제를 겪었습니다. 처음부터 둡니다.)
- **요청을 처리했으면 `response/{요청한 사람}/`에 회신 문서를 남깁니다** (회신 하나당 파일 하나, 파일명은 원본 요청과 동일하게). **폴더 이름이 이 문서를 읽을 사람**입니다 — 백엔드가 AI에게 보낸 요청은 `request/ai/`에, 그 회신은 `response/backend/`에 둡니다. **반영하지 않기로 한 경우에도 이유를 적어 회신합니다.** 자세한 규칙은 `response/README.md` 참조.
- PRD·기능명세서 자체를 수정해야 하는 결정(요구사항 변경, 스코프 조정)이 생기면 `00-context/prd.md`(필요 시 `spec.md`도)를 고치고, 그 사실을 팀 채널에 공유하세요. **필드를 추가·삭제·개명하려면 `02-architecture/api-contract.md`를 먼저 고치고 버전을 올린 뒤** 코드를 바꿉니다(계약서 §6 변경 절차).
- **스펙·계약 문서를 고쳤으면, 같은 작업 안에서 각자의 실행 계획 문서도 확인하세요.** 파생 문서는 자동으로 따라오지 않습니다. 해빙에서는 이 규칙이 없어 백엔드 문서 6개가 하루치만큼 낡은 적이 있습니다.
- 이 파일 맨 위 **수정 기록 배너**에 문서 변경 사항을 날짜와 함께 남깁니다. 지난주에 읽은 사람이 무엇이 바뀌었는지 배너만 보고 알 수 있어야 합니다.
