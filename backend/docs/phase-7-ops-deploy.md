# Phase 7 — 운영 · 배포

의존 없음 (오류 로깅은 처음부터 병행 가능). 데모 모드는 Phase 2 완료 후 의미가 생긴다.

## 체크리스트

- [ ] 데모 모드 플래그 — 계정 단위. `POST /api/session/start` 응답의 `demoMode`로 앱에 전달 — 근거: spec F11-01
- [ ] 구조화 오류 로깅 (`ops_error_log`) — **발화 내용을 절대 남기지 않는다** — 근거: spec F11-03, FR-092
- [ ] 평가 세트 실행 (P1) — 20쌍 고정 세트를 돌려 §1.4 지표(갭 방향 일치율, 재현성 편차, 위기 재현율, evidence 불일치 건수, 원문 외 태그 건수) 산출. **세트 자체는 AI서버가 갖고 있을 가능성이 높음 — 백엔드는 실행·집계만** — 근거: spec F11-04
- [ ] 배포 대상·플랫폼 결정 — `README.md` "스택" 표가 미확정. 결정되면 표부터 갱신
- [ ] 배포 환경변수 — Hume API 키, 카카오 앱 시크릿, JWT 서명 키, 내부 공유 시크릿, Supabase 접속 정보 — 전부 환경변수, 코드에 하드코딩 금지 — 근거: 루트 CLAUDE.md 절대 규칙

## 완료 기준

- TC-14 (데모 모드 on/off: on일 때만 대화 화면에 갭 노출 — 앱 쪽과 연동 확인)
- TC-20 (평가 세트 실행: 전 지표 1회 출력)
- 로그 전수 검사에서 `transcript` 문자열 0건
