# 아직 못 한 것 — 무엇이 막고 있나

> **작성 2026-09-05. 이 파일은 스냅샷이지 단일 출처가 아니다.**
> 항목의 진짜 출처는 **Phase 문서의 그 줄**이다. 여기를 고치고 Phase 문서를 안 고치면 **여기가 틀린다** — 반대는 괜찮다.
>
> ```sh
> grep -n "⏳" backend/docs/phase-*.md          # 끝내지 못한 완료 기준
> grep -n "^- \[ \]" backend/docs/phase-*.md   # 아직 안 한 작업
> grep -L "상태: ✅" docs/request/*/*.md        # 회신을 기다리는 요청
> ```

**Phase 1~6의 코드는 전부 서 있다.** 여기 남은 것은 **값 하나 또는 상대의 회신 하나**가 오면 그 자리에서 끝나는 것들이다. 그래서 "할 일" 순이 아니라 **막는 것** 순으로 묶었다 — 하나가 풀리면 몇 개가 같이 열리는지가 보여야 우선순위가 정해진다.

---

## ① Hume 계정 — AI가 만들어야 한다 (가장 많이 막고 있다)

**막는 것: 4건.** 사람이 브라우저에서 가입해야 하는 단계라 AI가 대신 못 한다고 회신했다(`response/backend/hume-account-setup.md`).

| 못 한 것 | 근거 | 오면 어떻게 되나 |
| --- | --- | --- |
| `POST /api/session/start`의 실제 토큰 발급 | `phase-2` 완료 기준 | 지금은 **503 `HUME_TOKEN_ISSUE_FAILED`**가 정상 동작이다. 키 3개를 `.env`에 넣으면 그 자리에서 끝난다 |
| **TC-02** — 실제 3분 대화 | `phase-2` | 세션 기록·요약 경로는 확인했다. 남은 건 Hume으로 진짜 말해보는 것 |
| **TC-03** — 앱 번들에 Hume 키가 없다 | `phase-2` | 서버 응답에 키가 없는 것은 구조상 확정(단기 토큰만 반환). **번들 확인은 앱 몫** |
| 앱이 `session/start` 응답만으로 EVI 연결 | `phase-2` | 응답은 이미 계약대로 나간다. `humeAccessToken`만 실물이 아니다 |

**필요한 값**: `HUME_API_KEY` · `HUME_SECRET_KEY` · `HUME_CONFIG_ID`
**요청 문서**: `../../docs/request/ai/hume-account-setup.md` (✅ 회신 왔으나 **키는 아직**)

> **결제 결정은 끝났다** — Creator 플랜. 490분 기준 Starter보다 싸다는 계산을 AI가 해왔다. **계정 가입만 남았다.**

## ② `ANTHROPIC_API_KEY` — AI가 넣어야 한다

**막는 것: 2건.** AI가 "코드는 서 있지만 한 번도 진짜 모델을 부른 적이 없다"고 회신했다.

| 못 한 것 | 근거 | 지금 상태 |
| --- | --- | --- |
| **진짜 문장으로 관찰 1건** | `phase-4` | **대역 서버로 성공 경로는 확인했다**(2026-09-05) — 판정·evidence·근거 연결·목록 노출까지. LLM이 쓴 문장만 못 봤다 |
| **진짜 문장으로 요약 1건** | `phase-2` | 같음. 대역 서버로 `summary`가 종료 응답에 실리는 것까지 확인 |

> **백엔드가 더 할 일은 없다.** 진짜 AI서버는 키가 없어 `422 SENTENCE_REJECTED(call_failed)`로 떨어지고, 우리 쪽은 그걸 **"문장이 없으면 관찰도 없다"**로 정확히 처리한다(템플릿 폴백 없음).

## ③ 앱 회신 2건

| 요청 | 못 하는 것 | 급한가 |
| --- | --- | --- |
| `request/app/chat-group-id.md` | **`resumedChatGroupId`가 항상 null** — 이어하기는 되는데 **이전 맥락이 복원되지 않는다** | **아니다.** F2-07은 P1이고 스코프 컷 3번 |
| `request/app/kakao-rest-key-switch.md` | **repo variable 이름 확정** (`KAKAO_JS_KEY` → `KAKAO_REST_KEY`) | 앱의 로그인 화면을 막는다. **등록은 팀장이 하면 되고 이름만 정하면 된다** |

## ④ AI 회신 1건 — 통합에서 나온 결함 2건

`request/ai/integration-round-1.md` (⏳, 2026-09-05)

| 결함 | 왜 중요한가 |
| --- | --- |
| **fail-closed 401 경로가 500으로 터진다** | 모르는 세션·`ended`·조회 실패에서 Hume에 401을 돌려주는 **유일한 방어선**이다(계약 §4). `error_log()` 인자 충돌. **정상 경로에서는 안 밟히는 코드라** 통합 전까지 안 보였다 |
| **로그에 `sessionId` 평문 7건** | 계약 §1-1이 "CLM 인증 수단이라 비밀과 동급"으로 못 박은 값이다. 백엔드는 같은 시간대 **0건**(`sessionRef` 해시 사용) |

> **백엔드를 막지는 않는다.** 다만 첫 번째는 위기 상황의 방어선이라 우선순위가 높다.

## ⑤ 앱 화면이 붙어야 하는 것

| 못 한 것 | 왜 지금 못 하나 |
| --- | --- |
| **실제 카카오 unlink 왕복** (`phase-6`) | 탈퇴는 계정을 지운다. **한 번 돌리면 되돌릴 수 없어** 임의로 실행할 수 없다. 앱의 탈퇴 화면이 붙은 뒤 도그푸딩 계정으로 본다 |

## ⑥ 실사용 로그가 쌓여야 하는 것

| 못 한 것 | 왜 |
| --- | --- |
| **TC-15** — 원문에 없는 태그가 저장돼 있지 않다 (`phase-3`) | **대조는 AI서버가 한다**(F6-02). 백엔드는 받은 대로 저장하므로 여기서 증명할 수 없다. 통합 후 실제 대화 로그로 확인한다 |

## ⑦ 배포 시점에 하는 것 (Phase 7)

| 못 한 것 | 근거 |
| --- | --- |
| **`TRANSCRIPT_ENC_KEY`(배포용) 저장소 밖 사본 1부** | `phase-1` 1-3-1 · `phase-3` 3-2. **세 시크릿 중 이것만** 오프라인 사본을 둔다 — 잃으면 도그푸딩 발화 전체가 복호화 불가다 |

**로컬 키는 이미 생성해 `.env`에 있다.** 배포용은 Phase 7에서 새로 만든다(로컬·배포 키 분리).

---

## 백엔드가 지금 당장 할 수 있는 것

**Phase 7뿐이다.** Supabase 생성 · Render 등록 · 환경변수 주입 · cron 킵얼라이브 · 데모 모드 운영.

그 외에는 **전부 위 ①~⑥ 중 하나를 기다린다.** 코드는 값이 오는 순간 그대로 돈다 — 기다리는 동안 백엔드가 더 만들 것은 없다.

> **여기에 "남은 것 N건"을 적지 않는다.** 손으로 센 숫자는 다음 작업이 끝나는 순간 틀리고, 틀린 줄 모른 채 "다 했다"의 근거가 된다 — 백엔드가 `tag-gap-endpoint.md`를 놓친 경위가 그것이었다(루트 `CLAUDE.md`). **세는 것은 맨 위 grep이 한다.**
